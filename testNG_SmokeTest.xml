<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd">

<suite name="Smoke Test Suite" verbose="1">
    <test name="Smoke Test">
        <packages>
            <package name="myapp.tests.smokeTest"/>
        </packages>
    </test>
</suite>

<!--
    1. xml files are used to Run the existing test cases
    2. suite>test>packages>groups>classes>methods
    3.verbose is optional. it is only the amount of info that you see on the console,bigger number;
    4. Save the xml file and then Run it.
    5. if the path is not correct, we see 'no test were found' error
-->

